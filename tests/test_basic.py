import pytest

def test_basic_initialization():
    """
    A simple test to ensure pytest is able to run correctly and exits with code 0.
    In the future, you can import your Flask app here and build unit tests using its test_client.
    """
    assert True, "Testing framework should execute properly."
